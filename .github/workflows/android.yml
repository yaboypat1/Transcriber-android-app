name: Android CI (self-hosted)

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

# Keep one run per branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    # Your runner should have these labels (see Section 3)
    runs-on: [self-hosted, linux, android]
    timeout-minutes: 45

    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # JDK 21 for modern AGP/Gradle
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      # Make sure Android SDK is present and licenses accepted on the runner.
      - name: Setup Android SDK (idempotent)
        uses: android-actions/setup-android@v3
        with:
          packages: |
            platform-tools
            cmdline-tools;latest
            platforms;android-35
            build-tools;35.0.0

      - name: Accept licenses (safe to re-run)
        run: yes | sdkmanager --licenses
        shell: bash

      # Validate and cache Gradle
      - name: Validate Gradle Wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: false

      # Build the phone + watch apps; keep it lean
      - name: Assemble Debug variants
        run: ./gradlew --no-daemon --stacktrace :app-mobile:assembleDebug :app-wear:assembleDebug

      # Run static analysis/tests
      - name: Run unit tests & detekt
        run: ./gradlew --no-daemon --stacktrace test detekt || true

      - name: Upload APKs
        uses: actions/upload-artifact@v4
        with:
          name: debug-apks
          if-no-files-found: warn
          path: |
            app-mobile/build/outputs/**/*.apk
            app-wear/build/outputs/**/*.apk
